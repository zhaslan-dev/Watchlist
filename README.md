```markdown
# 🎬 Watchlist – Telegram бот для совместной медиа-очереди

[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)](https://www.python.org/downloads/)
[![aiogram](https://img.shields.io/badge/aiogram-3.x-green.svg)](https://docs.aiogram.dev/)
[![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0-red.svg)](https://www.sqlalchemy.org/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

**Watchlist** — это Telegram‑бот, который помогает вместе с друзьями или второй половинкой выбирать, что посмотреть.  
Вы ищете фильмы через Кинопоиск, добавляете их в общую очередь, голосуете «За» или «Против», а бот сам подсчитывает голоса и присылает ссылки на онлайн‑кинотеатры.

---

## ✨ Возможности

- 🔍 **Поиск фильмов** по названию через Kinopoisk API с пагинацией  
- ➕ **Добавление** найденных фильмов в общую очередь  
- 👍 **Голосование** «За» или «Против» (можно менять мнение)  
- 📋 **Просмотр текущей очереди** с рейтингами и количеством голосов  
- 📜 **История просмотренных** фильмов с оценками (команда `/history`)  
- 🔗 **Интеграция с онлайн‑кинотеатрами** – кнопки для поиска на Кинопоиске, Иви, Okko  
- 🤖 **Инлайн‑клавиатуры** и простое меню  
- 💾 **Сохранение данных** в PostgreSQL / SQLite через SQLAlchemy (асинхронно)  
- 📊 **Метрики Prometheus** для мониторинга  
- 🐳 **Docker Compose** для лёгкого развёртывания  

---

## 🧠 Технологии

- **Python 3.11+**  
- **aiogram 3.x** – асинхронный фреймворк для Telegram  
- **SQLAlchemy 2.0** (async) – ORM  
- **asyncpg** / **aiosqlite** – драйверы БД  
- **Pydantic 2.0** – валидация данных  
- **loguru** – удобное логирование  
- **httpx** – асинхронные запросы к API  
- **redis** – кэширование (опционально)  
- **tenacity** – повторные попытки  
- **prometheus-client** – метрики  
- **poetry** – управление зависимостями  

---

## 🏛️ Архитектура

Проект построен на принципах **Clean Architecture** и **SOLID**:

- `domain/` – бизнес‑сущности и интерфейсы репозиториев  
- `application/` – сценарии использования (use cases)  
- `infrastructure/` – адаптеры (БД, API, Telegram бот)  
- `tests/` – тесты  

Это обеспечивает высокую тестируемость, независимость от внешних фреймворков и лёгкую замену компонентов.

---

## 🚀 Установка и запуск

### 📋 Требования

- Python 3.11 или выше  
- PostgreSQL (опционально, можно использовать SQLite)  
- Redis (опционально, для кэширования)  
- Telegram бот (токен от [@BotFather](https://t.me/BotFather))  
- API‑ключ [Kinopoisk Unofficial](https://kinopoiskapiunofficial.tech/)

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/yourname/watchlist.git
cd watchlist
```

### 2️⃣ Установка зависимостей

**Через Poetry (рекомендуется):**

```bash
poetry install
```

**Через pip:**

```bash
pip install -r requirements.txt
```

### 3️⃣ Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните основные параметры:

```env
BOT_TOKEN=ваш_токен_от_BotFather
KINOPOISK_API_KEY=ваш_ключ_от_kinopoiskapiunofficial
DATABASE_URL=sqlite+aiosqlite:///./watchlist.db   # или postgresql+asyncpg://...
REDIS_URL=redis://localhost:6379/0                # можно оставить пустым
LOG_LEVEL=INFO
VOTE_THRESHOLD=2
MIN_VOTERS=2
```

### 4️⃣ Запуск базы данных и кэша (если нужно)

**Вариант A – локальные службы (PostgreSQL, Redis):**

```bash
# PostgreSQL (Linux)
sudo systemctl start postgresql
# PostgreSQL (macOS)
pg_ctl -D /usr/local/var/postgres start

# Redis
redis-server
```

**Вариант B – Docker Compose (всё сразу):**

```bash
docker-compose up -d db redis
```

### 5️⃣ Миграции базы данных

```bash
alembic upgrade head
```

Если используется Docker Compose:

```bash
docker-compose exec bot alembic upgrade head
```

### 6️⃣ Запуск бота

**Локально:**

```bash
python -m Watchlist.main
```

**Через Docker Compose:**

```bash
docker-compose up -d bot
```

### 7️⃣ Проверка работы

- Откройте Telegram, найдите бота по его username.  
- Отправьте `/start` – бот ответит приветствием.  
- Используйте `/search` для поиска фильмов.  
- Добавьте фильм в очередь и проголосуйте.  
- `/queue` – текущая очередь.  
- `/history` – история просмотренных с оценками.  

### 8️⃣ Мониторинг

Метрики Prometheus доступны по адресу:  
`http://localhost:9090/metrics`  

- `bot_requests_total` – количество запросов по командам  
- `bot_votes_total` – количество голосов  
- `bot_search_duration_seconds` – время выполнения поиска  

Логи пишутся в `logs/bot.log` с ежедневной ротацией.

---

## 📖 Команды бота

| Команда     | Описание                              |
|-------------|---------------------------------------|
| `/start`    | Приветствие и главное меню            |
| `/search`   | Поиск фильмов по названию             |
| `/queue`    | Показать текущую очередь              |
| `/history`  | История просмотренных фильмов         |
| `/help`     | Справка                               |

Также доступны кнопки в главном меню: 🔍 Поиск, 📋 Очередь, ❓ Помощь.

---

## ⚖️ Голосование и принятие

- Каждый добавленный фильм имеет кнопки голосования 👍 За / 👎 Против.  
- Голос можно изменить в любой момент.  
- Фильм автоматически принимается, когда:
  - разница голосов **За** и **Против** достигает `VOTE_THRESHOLD`
  - общее количество проголосовавших не меньше `MIN_VOTERS`
- В **личных чатах** достаточно одного голоса «За» – фильм сразу принимается.  
- Принятый фильм сохраняется в историю, а в чат отправляется сообщение с кнопками для поиска фильма на онлайн‑кинотеатрах.  
- После просмотра бот спросит оценку (1–10) и добавит её в историю.

---

## 🧪 Тестирование

```bash
poetry run pytest -v
```

Тесты используют отдельную тестовую базу данных (см. `pyproject.toml`).

---

## 🗺️ Дальнейшее развитие

- [ ] Вынести уведомления в отдельный интерфейс `INotificationService` (DI)  
- [ ] Оптимизировать `/history` с использованием JOIN  
- [ ] Поддержка групповых чатов (отслеживание участников)  
- [ ] Рекомендации на основе истории просмотров  
- [ ] Прямые ссылки на фильмы в онлайн‑кинотеатрах (по ID)  
- [ ] Добавление жанров и рекомендации по жанрам  
- [ ] Webhook вместо поллинга для масштабирования  

---

## 📄 Лицензия

Проект распространяется под лицензией **MIT**. Подробности в файле [LICENSE](LICENSE).

---

**Watchlist** – делайте выбор фильмов вместе, быстро и удобно! 🎬
```
